<!DOCTYPE html>
<html lang="en">
{{>baseHead customTitle='สร้างโพสต์ใหม่ | ' }}
<body>
    {{>siteHeader}}
    <section class="section">
        <div class="container">
        <h2>สร้างโพสต์ใหม่</h2>
        <p class="mg-bt-large">ตั้งเรื่องที่สงสัยได้เลยทันที ตอนนี้!!</p>
        <form action="/p/new" method="post">
            <p>
                <input type="text" name="title" placeholder="หัวข้อ" value="{{values.title}}" >
            </p>
            <p>
                <textarea name="content" placeholder="เนื้อหา" rows="3">{{values.content}}</textarea>
            </p>
            <p>
                <input type="text" name="from" placeholder="จากคุณ" value="{{values.from}}">
            </p>
            <p>
                <input id="input-accepted" type="checkbox" name="accepted">
                <label class="user-select" for="input-accepted">โปรดยืนยันการตั้งคำถาม</label>
            </p>
            <p>
                <button class="button" type="submit">สร้างโพสต์</button>
            </p>
            {{#if errorMessage}}
                <p>{{ errorMessage }}</p>
            {{/if}}
        </form>
    </div>
    </section>
    {{>siteFooter}}
</body>
</html>